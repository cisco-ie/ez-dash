groups:
- name: testing
  rules:
  - alert: AngryCode
    expr: error > 0
    for: 1m
    labels:
      severity: critical
    annotations:
      description: "We got mad errors over here @ {{ $labels.service }}"
